factorial = func(n) : fact {
    if (n <= 1) {
        return 1;
    };
    return n * fact(n - 1);
}

i = 1;
cnt = ?;
arr = repeat(undef, cnt);
while (i <= cnt) {
    arr[i - 1] = factorial(i);
    i = i + 1;
}
print arr;